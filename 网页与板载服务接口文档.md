# 网页与板载服务接口文档

**版本**：1.0.0-draft
**日期**：2026-09-18
**状态**：基准文档（后续开发依据）
**适用系统**：役次元 · 积木编程编辑器（YCY-gateway-graphical）

---

## 1. 文档概述

### 1.1 目的

本文档定义网页前端（积木编辑器）与 RK3308H 板载后端服务之间的通信接口，作为双方开发的唯一契约基准。前端团队与板载服务团队以本文档为准对接，接口变更须同步修订本文档并升版本号。

### 1.2 接口范围

本文档覆盖 **网页客户端 ↔ RK3308H 板载后端服务** 的全部通信：

- HTTP 接口：积木 JSON 脚本上传
- WebSocket 通道：控制指令下行、设备状态与实时信息上行

**不在本文档范围内**：RK3308H 板载服务与 BLE 外设之间的蓝牙协议（属板载服务内部实现，见《设备信息.md》中设备参数定义；板载服务对外仅暴露本文档定义的 WebSocket/HTTP 接口）。

### 1.3 读者

前端开发、RK3308H 板载服务开发、联调测试。

### 1.4 术语

| 术语 | 说明 |
| --- | --- |
| 前端 / 网页客户端 | 部署在 RK3308H 本地的积木编辑器网页（index.html + js/*） |
| 板载服务 / 后端 | RK3308H 上运行的 Web 服务，承载 HTTP + WebSocket 与积木解释执行 |
| 外设 | 通过 BLE 蓝牙接入 RK3308H 的硬件设备（智能锁、电击器、灌肠机、跳蛋、榨精机） |
| 积木 JSON | 前端积木编辑器生成的自定义逻辑脚本，格式见《前端设计.md》【输出格式】 |
| 下行 | 网页客户端 → RK3308H 后端 |
| 上行 | RK3308H 后端 → 网页客户端 |

---

## 2. 系统架构说明

### 2.1 部署拓扑

```
局域网（Wi-Fi / 有线）
┌──────────────┐                    ┌─────────────────────────────────┐
│ 电脑 / 手机   │   HTTP+WebSocket   │        RK3308H 主板             │
│ 浏览器客户端  │ <----------------> │  ┌───────────────────────────┐  │
│ （积木编辑器）│    局域网访问IP     │  │  板载 Web 服务             │  │
└──────────────┘                    │  │  · HTTP 静态资源/脚本上传  │  │
                                    │  │  · WebSocket 实时通道      │  │
                                    │  │  · 积木 JSON 解析/解释执行 │  │
                                    │  └───────────┬───────────────┘  │
                                    │              │ 板载 BLE 蓝牙    │
                                    │  ┌───────────▼───────────────┐  │
                                    │  │  BLE 外设管理 / 状态采集  │  │
                                    │  └───────────┬───────────────┘  │
                                    └──────────────┼──────────────────┘
                                                   │ BLE
                                    ┌──────────────▼──────────────────┐
                                    │ 外设：智能锁 / 电击器 / 灌肠机  │
                                    │       跳蛋 / 榨精机             │
                                    └─────────────────────────────────┘
```

### 2.2 职责划分

| 职责 | 归属 |
| --- | --- |
| UI 渲染（积木编辑器、设备面板） | 网页前端 |
| 积木拖拽编辑、生成积木 JSON | 网页前端 |
| 下发控制指令（start / stop）、上传脚本 | 网页前端 |
| 静态资源服务、HTTP 接口 | RK3308H 板载服务 |
| WebSocket 实时通道 | RK3308H 板载服务 |
| 积木 JSON 解析、积木逻辑解释执行 | RK3308H 板载服务 |
| BLE 蓝牙外设管理（扫描/连接/断开） | RK3308H 板载服务 |
| 外设状态采集与实时上报 | RK3308H 板载服务 |
| **直接操作蓝牙硬件** | **仅板载服务**。网页不直接访问 BLE |

### 2.3 关键设计约束

1. **网页不直接操作蓝牙硬件**。前端对设备的全部控制与读取都经由板载服务转发。
2. **WebSocket 用于双向实时消息，消息体统一 JSON**；积木 JSON 脚本上传使用 HTTP POST。
3. 单客户端假设（v1）：同一时刻最多一个浏览器客户端持有控制权；多客户端并发控制列入后续版本。
4. 外设能力集（opcode、参数、状态值）以《前端设计.md》积木库与《设备信息.md》为准，板载服务按此解析执行。

---

## 3. 通信分层说明

| 层 | 链路 | 协议 | 说明 |
| --- | --- | --- | --- |
| L1 | 浏览器客户端 ↔ RK3308H 板载 Web 服务 | HTTP/1.1 + WebSocket | 局域网，RK3308H IP 访问；本文档定义范围 |
| L2 | RK3308H 板载服务 ↔ 外设 | 板载 BLE 蓝牙 | 板载服务内部实现，不在本文档范围 |

本文档仅约定 L1。L2 的蓝牙协议由板载服务团队内部定义，但其**语义必须**与本文档 §6 / §7 的字段一一对应（外设清单、实时参数、连接状态）。

---

## 4. 通信协议总述

### 4.1 基础约定

| 项 | 约定 |
| --- | --- |
| 传输 | HTTP/1.1（脚本上传、静态资源）；WebSocket（实时双向） |
| 编码 | UTF-8 |
| 消息体 | 统一 JSON（`Content-Type: application/json`；WebSocket text frame） |
| WebSocket URL | `ws://<RK3308H_IP>:<PORT>/ws` |
| HTTP 基址 | `http://<RK3308H_IP>:<PORT>` |
| 心跳 | 客户端每 10s 发送 `{"type":"ping"}`，服务端回 `{"type":"pong"}`；30s 未收到 pong 判定链路断开 |
| 时间戳 | 毫秒级 Unix 时间戳（UTC），字段名 `ts` |
| 字段风格 | snake_case |

### 4.2 端口与路径规划

| 路径 | 方法/用途 | 说明 |
| --- | --- | --- |
| `/` | HTTP GET | 网页静态资源（index.html / css / js） |
| `/api/scripts` | HTTP POST | 上传积木 JSON 脚本（§8） |
| `/api/scripts/{name}` | HTTP GET（预留） | 获取已保存脚本，v1 可不实现 |
| `/ws` | WebSocket | 实时双向通道（§5–§7） |

端口号由部署配置决定，前端不得硬编码；前端从 `window.location` 推导 WebSocket 地址（同源升级 `http→ws`）。

---

## 5. WebSocket 消息类型枚举定义

### 5.1 下行（网页客户端 → 板载服务）

| type | 方向 | 说明 |
| --- | --- | --- |
| `ping` | 下行 | 心跳 |
| `start` | 下行 | 加载已保存 JSON，启动积木逻辑执行 |
| `stop` | 下行 | 终止当前正在运行的积木逻辑 |

### 5.2 上行（板载服务 → 网页客户端）

| type | 方向 | 说明 |
| --- | --- | --- |
| `pong` | 上行 | 心跳应答 |
| `device_status` | 上行 | 蓝牙外设连接状态变化（某外设接入/断开） |
| `script_status` | 上行 | 脚本运行状态变化（running / stopped / parse_error）+ 链路异常报错 |
| `device_list` | 上行 | 已接入蓝牙外设清单（全量，用于设备面板渲染） |
| `device_data` | 上行 | 外设实时参数（增量推送） |
| `error` | 上行 | 链路/服务异常报错信息 |

### 5.3 枚举值定义

**script_status.state**

| 值 | 说明 |
| --- | --- |
| `running` | 积木逻辑执行中 |
| `stopped` | 已停止（用户 stop 或执行完成） |
| `parse_error` | 积木 JSON 解析失败 |

**device_status.state**

| 值 | 说明 |
| --- | --- |
| `connected` | 外设已接入 |
| `disconnected` | 外设已断开 |

**device_list[].category** — 与《前端设计.md》积木分类一致：

| 值 | 外设 |
| --- | --- |
| `device_lock` | 智能锁 |
| `device_tens` | 电击器 |
| `device_enema` | 灌肠机 |
| `device_vibe_a` | 跳蛋 |
| `device_vibe_b` | 榨精机 |

---

## 6. WebSocket 消息结构体

所有消息共用信封字段：`type`（string，必填）、`ts`（number，毫秒时间戳，必填）、`code`（number，仅应答/错误消息）、`msg`（string，人读信息，仅错误）。

### 6.1 下行结构体

**ping**

```json
{ "type": "ping", "ts": 1758200000000 }
```

**start** — 加载已保存 JSON 并启动执行。

```json
{
  "type": "start",
  "ts": 1758200000000,
  "script": "logic.json",
  "run_id": "r-20260918-001"
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| script | string | 否 | 已保存脚本文件名；缺省用最近一次 HTTP 上传的脚本 |
| run_id | string | 否 | 前端生成的本次执行标识（幂等/去重）；服务端在 script_status 中回带 |

JSON Schema（start）：

```json
{
  "type": "object",
  "required": ["type", "ts"],
  "properties": {
    "type": { "const": "start" },
    "ts": { "type": "number" },
    "script": { "type": "string", "pattern": "^[A-Za-z0-9_-]+\\.json$" },
    "run_id": { "type": "string", "maxLength": 64 }
  },
  "additionalProperties": false
}
```

**stop**

```json
{ "type": "stop", "ts": 1758200000000, "run_id": "r-20260918-001" }
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| run_id | string | 否 | 要终止的执行标识；缺省终止当前运行 |

### 6.2 上行结构体

**pong**

```json
{ "type": "pong", "ts": 1758200000000 }
```

**device_status** — 单个外设连接状态变化。

```json
{
  "type": "device_status",
  "ts": 1758200000000,
  "data": {
    "device_id": "tens-01",
    "category": "device_tens",
    "name": "电击器-01",
    "state": "connected"
  }
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| data.device_id | string | 是 | 外设唯一标识（BLE 地址或服务端分配 ID） |
| data.category | string | 是 | 枚举见 §5.3 |
| data.name | string | 否 | 外设显示名 |
| data.state | string | 是 | `connected` / `disconnected` |

**script_status** — 脚本运行状态变化与报错。

```json
{
  "type": "script_status",
  "ts": 1758200000000,
  "data": {
    "run_id": "r-20260918-001",
    "state": "running"
  }
}
```

parse_error 示例：

```json
{
  "type": "script_status",
  "ts": 1758200000000,
  "code": 3001,
  "data": {
    "run_id": "r-20260918-001",
    "state": "parse_error",
    "error": "未知 opcode: vibe_a_frequency",
    "line": 12
  }
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| data.run_id | string | 否 | 关联的执行标识 |
| data.state | string | 是 | `running` / `stopped` / `parse_error` |
| data.error | string | 否 | 解析失败/链路异常报错信息 |
| data.line | number | 否 | 解析失败时的脚本位置（若可定位） |

**device_list** — 全量外设清单（连接变化后主动推送；客户端也可通过 `start` 后等待此消息刷新面板）。

```json
{
  "type": "device_list",
  "ts": 1758200000000,
  "data": {
    "devices": [
      { "device_id": "tens-01", "category": "device_tens", "name": "电击器-01", "online": 1 },
      { "device_id": "lock-01", "category": "device_lock", "name": "智能锁-01", "online": 1 }
    ]
  }
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| data.devices | array | 是 | 外设数组；`online`：1 在线 / 0 离线 |

**device_data** — 外设实时参数（增量；字段集以《设备信息.md》为准，以下是示例形态）。

```json
{
  "type": "device_data",
  "ts": 1758200000000,
  "data": {
    "device_id": "tens-01",
    "params": {
      "tens_omega": 120.5,
      "tens_alpha": 8.2,
      "tens_angle": 45,
      "tens_step": 1024,
      "tens_power": 87,
      "tens_connection_status": 1
    }
  }
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| data.device_id | string | 是 | 外设唯一标识 |
| data.params | object | 是 | 实时参数键值对；键名与《前端设计.md》状态值积木 opcode 一致（tens_omega / tens_alpha / tens_angle / tens_step / tens_power / tens_connection_status / enema_pressure / lock_state 等） |

**error** — 链路/服务异常。

```json
{
  "type": "error",
  "ts": 1758200000000,
  "code": 5001,
  "msg": "WebSocket 服务内部错误"
}
```

---

## 7. HTTP 接口定义（上传积木 JSON 脚本）

### 7.1 POST /api/scripts

上传积木编辑器生成的逻辑 JSON 文件，保存到 RK3308H 本地。

**请求**

| 项 | 值 |
| --- | --- |
| 方法 | POST |
| 路径 | /api/scripts |
| Content-Type | application/json; charset=utf-8 |
| Body | 积木 JSON（格式见《前端设计.md》【输出格式】） |

请求体 Schema（顶层）：

```json
{
  "type": "object",
  "required": ["version", "program"],
  "properties": {
    "version": { "type": "string", "const": "1.0" },
    "program": { "type": "array", "minItems": 1 },
    "variables": { "type": "object" }
  },
  "additionalProperties": false
}
```

**响应 200**

```json
{
  "code": 0,
  "msg": "ok",
  "data": { "script": "logic-1758200000000.json", "size": 1042 }
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| data.script | string | 服务端保存的文件名（后续 `start` 可引用） |
| data.size | number | 文件字节数 |

### 7.2 GET /api/scripts/{name}（预留）

获取已保存脚本内容。v1 可不实现；实现时响应体为脚本 JSON 原文。

---

## 8. 请求/响应 JSON 示例

### 8.1 HTTP：上传脚本（完整往返）

请求：

```
POST /api/scripts HTTP/1.1
Host: 192.168.1.100:8080
Content-Type: application/json; charset=utf-8
```

```json
{
  "version": "1.0",
  "program": [
    {
      "opcode": "when_start",
      "id": "blk_1",
      "next": {
        "opcode": "repeat_times",
        "id": "blk_2",
        "inputs": { "N": 10 },
        "body": [
          { "opcode": "tens_channel_set", "id": "blk_3", "inputs": { "ID": 1, "CH": "A", "N": 50 } },
          { "opcode": "wait_seconds", "id": "blk_4", "inputs": { "N": 1, "UNIT": "秒" } }
        ]
      }
    }
  ],
  "variables": {}
}
```

成功响应：

```json
{ "code": 0, "msg": "ok", "data": { "script": "logic-1758200000000.json", "size": 638 } }
```

失败响应（脚本校验不通过）：

```json
{ "code": 3001, "msg": "未知 opcode: vibe_a_frequency", "data": { "line": 12 } }
```

### 8.2 WebSocket：启动执行（完整往返）

客户端 → 服务端：

```json
{ "type": "start", "ts": 1758200000000, "script": "logic-1758200000000.json", "run_id": "r-20260918-001" }
```

服务端 → 客户端（按序推送）：

```json
{ "type": "script_status", "ts": 1758200000100, "data": { "run_id": "r-20260918-001", "state": "running" } }
```

```json
{ "type": "device_data", "ts": 1758200000150, "data": { "device_id": "tens-01", "params": { "tens_power": 87, "tens_angle": 45 } } }
```

### 8.3 WebSocket：终止执行

客户端 → 服务端：

```json
{ "type": "stop", "ts": 1758200010000, "run_id": "r-20260918-001" }
```

服务端 → 客户端：

```json
{ "type": "script_status", "ts": 1758200010100, "data": { "run_id": "r-20260918-001", "state": "stopped" } }
```

### 8.4 WebSocket：解析失败

客户端 `start` 后脚本损坏时：

```json
{ "type": "script_status", "ts": 1758200000200, "code": 3001, "data": { "run_id": "r-20260918-001", "state": "parse_error", "error": "未知 opcode: vibe_a_frequency", "line": 12 } }
```

### 8.5 WebSocket：外设接入/断开

```json
{ "type": "device_status", "ts": 1758200005000, "data": { "device_id": "enema-01", "category": "device_enema", "name": "灌肠机-01", "state": "disconnected" } }
```

```json
{ "type": "device_list", "ts": 1758200005010, "data": { "devices": [ { "device_id": "tens-01", "category": "device_tens", "name": "电击器-01", "online": 1 } ] } }
```

---

## 9. 状态码与错误码定义

### 9.1 HTTP 状态码

| 状态码 | 说明 |
| --- | --- |
| 200 | 成功（业务结果看 `code`） |
| 400 | 请求格式错误（非 JSON / 缺字段） |
| 404 | 路径不存在 |
| 405 | 方法不允许 |
| 413 | 请求体过大（脚本 > 1MB） |
| 500 | 服务内部错误 |

### 9.2 业务错误码（独立定义）

| code | 名称 | 场景 | 建议前端处理 |
| --- | --- | --- | --- |
| 0 | OK | 成功 | — |
| 1001 | BAD_REQUEST | 请求格式错误/缺必填字段 | 提示「请求格式错误」 |
| 1002 | PAYLOAD_TOO_LARGE | 脚本超过 1MB | 提示「脚本过大」 |
| 2001 | WS_NOT_READY | WebSocket 未连接时收到无效消息 | 忽略 |
| 2002 | WS_UNKNOWN_TYPE | 未知消息 type | 忽略并上报 |
| 2003 | WS_AUTH_FAILED | 连接鉴权失败（预留，v1 无鉴权） | 提示「连接被拒绝」 |
| 3001 | SCRIPT_PARSE_ERROR | 积木 JSON 解析失败（未知 opcode / 结构错误） | 弹出错误信息（编辑器已有 err-item 展示） |
| 3002 | SCRIPT_VALIDATION_ERROR | 积木逻辑校验不通过（条件缺失等） | 弹出错误信息 |
| 3003 | SCRIPT_NOT_FOUND | start 指定的脚本文件不存在 | 提示「脚本不存在，请先上传」 |
| 3004 | SCRIPT_ALREADY_RUNNING | 已有脚本在运行（未 stop 又 start） | 先 stop 或直接覆盖（v1 行为：覆盖并重启） |
| 4001 | DEVICE_NOT_FOUND | 指令引用的外设 device_id 不存在 | 提示「设备未连接」 |
| 4002 | DEVICE_DISCONNECTED | 执行中外设断开 | script_status 停止 + 设备面板标记离线 |
| 4003 | DEVICE_BUSY | 外设忙（BLE 写入冲突） | 重试一次，仍失败提示 |
| 5001 | INTERNAL_ERROR | 板载服务内部错误 | 提示「服务异常，请重试」 |
| 5002 | BLE_ERROR | 板载 BLE 模块异常 | 提示「蓝牙模块异常」 |

错误响应统一信封：`{ "code": <int>, "msg": "<人读信息>", "data": { ... 可选上下文 } }`。WebSocket 错误同样携带 `code` / `msg`。

---

## 10. 通信时序说明

### 10.1 正常时序：连接 → 上传 → 启动 → 执行 → 停止

```
浏览器客户端                        RK3308H 板载服务                 BLE 外设
     │                                     │                            │
     │  1. GET /（加载网页静态资源）        │                            │
     │ <----------------------------------> │                            │
     │                                     │                            │
     │  2. WS 握手 GET /ws（Upgrade）       │                            │
     │ <==================================> │  WebSocket 建立            │
     │                                     │                            │
     │  3. {"type":"ping"}                  │                            │
     │ ----------------------------------> │                            │
     │  <------------------------------ {"type":"pong"}                   │
     │      （此后每 10s 心跳一次）         │                            │
     │                                     │                            │
     │  4. POST /api/scripts（积木JSON）    │                            │
     │ ----------------------------------> │ 保存到本地                  │
     │  <------------------------------ 200 {"code":0,"data":{...}}      │
     │                                     │                            │
     │  5. {"type":"start","run_id":...}    │                            │
     │ ----------------------------------> │ 解析积木JSON                │
     │  <------------------------------ {"type":"script_status",         │
     │      "state":"running"}              │                            │
     │                                     │  6. BLE 指令下发循环        │
     │                                     │ ==========================> │
     │  7. {"type":"device_data",...}       │ <====== 实时参数 =========  │
     │ <---------------------------------- │ 采集并增量推送              │
     │      （执行期间持续推送）            │                            │
     │                                     │                            │
     │  8. {"type":"stop","run_id":...}     │                            │
     │ ----------------------------------> │ 终止解释执行                │
     │                                     │ =========> BLE 停止指令     │
     │  <------------------------------ {"type":"script_status",         │
     │      "state":"stopped"}              │                            │
     │                                     │                            │
```

### 10.2 异常时序：外设中途断开

```
浏览器客户端                        RK3308H 板载服务                 BLE 外设
     │                                     │                            │
     │      （脚本 running 中）             │ <×× 连接丢失 ××            │
     │                                     │                            │
     │  1. {"type":"device_status",         │                            │
     │      "state":"disconnected"}         │                            │
     │ <---------------------------------- │                            │
     │  2. {"type":"device_list",...}       │   全量清单刷新              │
     │ <---------------------------------- │                            │
     │  3. {"type":"script_status",         │   依赖该外设的逻辑终止      │
     │      "state":"stopped",              │                            │
     │       code:4002}                     │                            │
     │ <---------------------------------- │                            │
     │  4. 前端：设备面板标记离线 +          │                            │
     │     停止状态提示                      │                            │
```

### 10.3 异常时序：脚本解析失败

```
浏览器客户端                        RK3308H 板载服务
     │                                     │
     │  {"type":"start","script":...}       │
     │ ----------------------------------> │
     │                                     │ 解析失败（未知 opcode）
     │  <------------------------------ {"type":"script_status",
     │      "state":"parse_error",          │
     │       "error":"未知 opcode: ...",    │
     │       "line":12}                     │
     │  前端：弹窗展示错误信息（err-item），  │
     │  不进入运行态                         │
```

### 10.4 心跳与断线重连

```
客户端每 10s ── ping ──> 服务端 ── pong ──> 客户端
  · 30s 未收到 pong → 判定断链，前端状态切「异常」（device.js error 态）
  · 断链后前端指数退避重连（1s/2s/4s/…上限 30s），重连成功后：
    1) 重新发送 start 前先查询 script_status（或直接重新 start，服务端幂等）
    2) 等待 device_list 全量推送刷新设备面板
```

---

## 11. 异常处理说明

### 11.1 前端处理矩阵

| 异常 | 检测方式 | 前端行为 | 界面反馈 |
| --- | --- | --- | --- |
| WebSocket 连接失败 | ws.onerror / onclose | 指数退避重连 | 状态栏「异常」 |
| 心跳超时（30s 无 pong） | 定时器比对 | 主动断开并重连 | 状态栏「异常」 |
| 脚本上传失败（code≠0） | HTTP 响应 code | 不发起 start，展示 msg | 弹窗/err-item 红色错误 |
| 解析失败（script_status parse_error） | WS 推送 | 不进入运行态，展示 error/line | 弹窗展示错误信息 |
| 外设断开（device_status disconnected） | WS 推送 | 刷新设备面板；运行态则等待 script_status | 设备面板标记离线 |
| 执行中外设断开（code 4002） | WS 推送 | 停止执行展示 | 提示「设备断开，已停止」 |
| 收到未知消息 type | WS 推送 | 忽略（可 console.warn） | 无 |
| 重复 start（code 3004） | WS/HTTP 响应 | v1 接受覆盖重启；或先 stop | 无需用户介入 |

### 11.2 后端处理矩阵

| 异常 | 处理 |
| --- | --- |
| 积木 JSON 解析失败 | 推送 script_status parse_error（code 3001），不执行任何 BLE 指令 |
| 积木逻辑执行中出错（外设无响应等） | 终止执行，推送 script_status stopped（code 4002/5001）+ error 信息 |
| BLE 模块异常 | 推送 error（code 5002），拒绝 start |
| 客户端异常断开 | 停止正在执行的脚本（安全优先），清理会话状态 |
| 脚本文件写入失败 | HTTP 返回 500（code 5001） |
| 恶意/超大脚本 | 413（code 1002）；解析深度限制见《前端设计.md》MAX_IMPORT_DEPTH |

### 11.3 安全与幂等

1. **安全优先**：任何异常路径下，板载服务必须先停止外设动作（BLE 安全停机指令），再通知前端。
2. **start 幂等**：相同 `run_id` 重复 start，服务端忽略并回当前状态；不同 run_id 在运行中到达，v1 覆盖重启（code 3004 语义）。
3. **stop 幂等**：重复 stop 或 stop 不存在的 run_id，回 `stopped`（code 0），不报错。
4. 文件名约束：`script` 参数仅允许 `[A-Za-z0-9_-]+\.json`，防止路径穿越。

---

## 12. 版本与变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| 1.0.0-draft | 2026-09-18 | 首版基准：HTTP 脚本上传 + WebSocket 控制指令/状态推送/实时数据 |

> 联调前双方确认：端口号、`device_id` 分配规则、`device_data.params` 字段全集（以《设备信息.md》最终版为准）。确认后本文档升为 1.0.0 正式版。
