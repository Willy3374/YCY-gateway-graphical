# 2026-09-17

## 项目：gateway_graphical — 图形化编程引擎（积木编辑器）

工作目录 `C:\Users\ywy\Desktop\gateway_graphical`（= 本次会话 workspace）。纯前端零依赖，双击 `index.html` 即可用。
定位：用户拖拽积木 → 编译成自定义逻辑 JSON；**未来部署到 RK3308 硬件网关，用于生成可执行文件**。这个最终目标很重要，所有设计都要考虑网关侧复用。

### 文件结构
- `index.html` — 三栏布局（积木库 / 画布 / 右侧连接状态栏）+ 输出弹层 + 命名弹窗 + 开始弹窗
- `css/style.css` — 变量化主题，`:root` 深色 / `[data-theme="light"]` 浅色
- `js/blocks.js` — 积木库唯一数据源：39 个 opcode、分类、参数（name/type/default/options）
- `js/examples.js` — 内置示例工程（附件 JSON 原样落地，名「电击器脉冲」）
- `js/engine.js` — 图形状态 → 逻辑 JSON（`GP.toSpec`）
- `js/workspace.js` — 画布渲染 + 拖拽（`GPWorkspace`）
- `js/theme.js` — 外观三态（`GPTheme`）
- `js/device.js` — 右侧连接状态栏（`GPDevice`）
- `js/main.js` — 工具栏、命名保存、示例、校验
- `tools/serve.js` — 静态服务器 + `/api/save`（真写盘）+ `/api/list`
- `tools/*.test.js` / `tools/check-*.js` — 自检脚本

### 积木规范（约定，勿杜撰 opcode）
- 字段：opcode(snake_case) / category / inputs[{name,type,default,options}]
- 类型：number | string | boolean | enum | variable | condition_block | statement_block
- 输出 JSON：`{version:"1.0", program:[...], variables:{}}`
- **嵌套用 body / else（数组），线性顺序用 next（对象）**，条件/运算/值以内联对象嵌进 inputs 并带 `returns`
- 条件/运算/值积木**不单独成块**，只作内联对象
- 默认值：ID=1，强度=0，时间=1 秒
- 事件积木只能在顶层且各自开新链；数字槽只收 returns=number，条件槽只收 returns=boolean

### 本轮完成的改动（按时间顺序）
1. **搭出整个编辑器**（blocks/engine/workspace/main + serve）
2. **外观切换**：跟随系统 / 浅色 / 深色，`data-theme` 属性驱动，`<head>` 内联脚本防首屏闪烁
3. **logo 全站替换**为 `logo.jpg`（favicon + 顶栏 + 弹层标题）
4. **深色主题 logo 反色**：`tools/make-dark-logo.ps1` 生成 `logo-dark.png`（invert + hue-rotate180 + 提亮，并把白底转透明）
5. **修 bug：顶层堆叠顺序反了** — 见下方"踩过的坑"
6. **载入示例** = 附件 JSON；**执行时命名弹窗**，默认名 `YYYYMMDDHHmm`，保存到网关 index.html 同目录
7. **右侧连接状态栏**，默认占位「未连接到设备」
8. **顶部「执行」改「生成」**，右侧加「开始」按钮，点击弹「未连接到网关」占位

### 踩过的坑（重要，别重复）
- **顶层拖放顺序反**：`insertionIndex()` 只从 `listEl.children` 取子块，但顶层画布没有独立 `.list` 容器（`listEl === null`）→ `kids` 恒空 → 索引恒 0 → 每块都插到最顶端。修法：`blockElsIn(listEl || rootEl)`。抽出纯函数 `insertIndexFromMids(mids, clientY)` 便于测试。
- **`importNode` 不认识 `next`**：附件程序整条主链靠 `next` 串联，原实现只读 body/else，载入后只剩第一块。已改为沿 `next` 展开成线序（加 5000 深度环保护）。
- **`GPDevice.init` 监听泄漏**：每次 init 都给刷新按钮再加一个 click 监听。改为模块级 `probe` + `refreshBound` 守卫，只绑一次。**测试抓出来的真 bug，不是测试问题。**
- **PowerShell 引号地狱**：内联 `node -e "..."` 里的中文/引号会被 GBK 与转义搞崩（多次误判为产品问题）。教训：验证逻辑一律写成 `tools/*.js` 文件再跑，别用内联脚本。
- **`tools/serve.js` 缺 `.jpg` MIME**：logo 被当 octet-stream 发。
- **读图限制**：当前模型不支持图片，`read` 图片会提示 omitted — 改用像素采样（System.Drawing）判断图内容。

### 验证习惯（本项目已建立，继续保持）
改动后必跑：
- `node --check` 各 js
- `node tools/smoke.js`（引擎自检）
- `node tools/order.test.js`（17 项，顺序）
- `node tools/dom.drag.test.js`（4 项，最小 DOM 桩驱动真实 handleDrop 路径）
- `node tools/example.test.js`（30 项，示例往返：JSON→积木→JSON 结构一致）
- `node tools/device.test.js`（32 项，状态栏）
- `node tools/check-markup.js`、`node tools/check-served.js`（标记 + 服务器实际返回字节）
- CSS 括号配平
**并做反向验证**：把旧逻辑放进临时副本跑测试，确认测试真能抓住该 bug（避免写永远绿的测试）。
**注意**：`.test.js` 里若设了定时器/轮询，必须清理，否则进程不退出、留下僵尸。

### 当前运行状态
- 服务：`tools/serve.js`，端口 **4317**，后台会话 `warm-reef`（shell pid 192 / node pid 21156），保存目录 = 项目根
- 访问：http://127.0.0.1:4317/
- 保存产物目前直接落在项目根目录（与 index.html 同级）；可用环境变量 `GP_OUT_DIR` 改目录

### 仓库与文档（晚些时候补记）
- 仓库：`github.com/Willy3374/YCY-gateway-graphical`，分支 `main`，当前工作目录 `C:\Users\ywy\Desktop\_0_Work\YCY-gateway-graphical`（上面写的旧路径已作废）
- **README.md 重写完成**：定位明确为「基于 RK3308H 物联网网关的图形化编程界面 → 生成逻辑 JSON → 板载 C 解释器解释执行 → 蓝牙调外设并接收回传参数」。含架构图、目录表、积木库全表、JSON 格式规范、自检命令、接入缺口、开发约定、文档维护要求
- 事实核对：积木库实为 **41 个 opcode**（事件2/控制7/锁1/电击器2/灌肠机3/跳蛋2/榨精机2/运算14/值8；形态：hat 2 / cblock 4 / reporter 19 / stack 16），上面「39 个」是早期数字
- 全量自检通过（syntax / smoke / order / dom.drag / example / device / check-markup / check-served）
- 服务器本轮已重启：后台会话 `vivid-ridge`（pid 22636），端口 4317
- 约定：**后续任何优化都要同步改 README**（用户明确要求） <!-- project: github.com/Willy3374/YCY-gateway-graphical -->

### 待办 / 下一步（用户可能继续提）
- 「开始」按钮目前**只是占位弹窗，不下发任何东西**；真实下发入口在 `main.js` 的 `openStartDialog()`
- 右侧状态栏接真机：`GPDevice.init({ probe: () => fetch('/api/device'), pollMs: 5000 })`
- 已提议但用户未表态：最近生成面板（读 `/api/list`）、快捷键循环切主题、拖到 C 形积木标题行自动纳入凹槽、设备列表分组
